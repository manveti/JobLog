<Window x:Class="JobLog.LinkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JobLog"
        mc:Ignorable="d"
        FocusManager.FocusedElement="{Binding ElementName=url_box}"
        Title="Link" Width="300" Height="100" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Margin="1" Content="URL:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Margin="1" Name="url_box"/>

        <Button Grid.Row="2" Grid.Column="2" Margin="1" Content="OK" Click="doOk" IsDefault="True"/>
        <Button Grid.Row="2" Grid.Column="3" Margin="1" Content="Cancel" Click="doCancel" IsCancel="True"/>
    </Grid>
</Window>
